import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ra.duoc.cl',
  appName: 'registroApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
